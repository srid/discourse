name: discourse

framework:
  type: buildpack
  runtime: ruby19

services:
  ${name}-db:    postgresql
  ${name}-redis: redis
  ${name}-cache: filesystem

# disabled because of https://github.com/discourse/discourse/issues/50
# hooks:
#   post-staging:
#     - bundle exec rake db:seed_fu
